name: My First workflow

on:
  push:

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: |
          echo "Ritesh Rana"

  say-bye:
    needs: say-hello
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: |
          echo "Bye!"
          echo "Ritesh Rana pull request"